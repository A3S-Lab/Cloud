name: E0 clean-host release

on:
  push:
    branches:
      - main
    paths:
      - .github/workflows/e0-clean-host-release.yml
      - Cargo.lock
      - Cargo.toml
      - README.md
      - config/**
      - crates/**
      - docs/**
      - deploy/web/**
      - justfile
      - tools/release-conformance/**
      - tools/gateway-conformance/gateway-revision
      - tools/runtime-conformance/runtime-revision
      - tools/dev/**
      - tools/web/**
      - web/**
  pull_request:
    paths:
      - .github/workflows/e0-clean-host-release.yml
      - Cargo.lock
      - Cargo.toml
      - README.md
      - config/**
      - crates/**
      - docs/**
      - deploy/web/**
      - justfile
      - tools/release-conformance/**
      - tools/gateway-conformance/gateway-revision
      - tools/runtime-conformance/runtime-revision
      - tools/dev/**
      - tools/web/**
      - web/**
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: e0-clean-host-release-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

jobs:
  clean-host-release:
    name: Clean-host E0 release
    runs-on: ubuntu-24.04
    timeout-minutes: 45
    defaults:
      run:
        shell: bash
    steps:
      - name: Checkout Cloud
        uses: actions/checkout@v4
        with:
          path: apps/cloud

      - name: Read pinned Runtime revision
        id: runtime_revision
        working-directory: apps/cloud
        run: |
          revision=$(<tools/runtime-conformance/runtime-revision)
          [[ $revision =~ ^[0-9a-f]{40}$ ]]
          echo "revision=$revision" >>"$GITHUB_OUTPUT"

      - name: Read pinned Gateway revision
        id: gateway_revision
        working-directory: apps/cloud
        run: |
          revision=$(<tools/gateway-conformance/gateway-revision)
          [[ $revision =~ ^[0-9a-f]{40}$ ]]
          echo "revision=$revision" >>"$GITHUB_OUTPUT"

      - name: Checkout Runtime
        uses: actions/checkout@v4
        with:
          repository: A3S-Lab/Runtime
          ref: ${{ steps.runtime_revision.outputs.revision }}
          path: crates/runtime

      - name: Checkout Gateway
        uses: actions/checkout@v4
        with:
          repository: A3S-Lab/Gateway
          ref: ${{ steps.gateway_revision.outputs.revision }}
          path: crates/gateway

      - name: Install stable Rust
        uses: actions-rust-lang/setup-rust-toolchain@v1
        with:
          toolchain: stable
          cache: false

      - name: Build pinned A3S Gateway
        run: |
          cargo build --manifest-path crates/gateway/Cargo.toml \
            --release --locked --bin a3s-gateway
          echo "$GITHUB_WORKSPACE/crates/gateway/target/release" >>"$GITHUB_PATH"

      - name: Validate Cloud Web Gateway profile
        working-directory: apps/cloud
        run: a3s-gateway validate --config deploy/web/gateway.acl

      - name: Verify disposable Linux release host
        env:
          EVIDENCE_DIR: ${{ runner.temp }}/a3s-cloud-clean-host-e0-evidence
        run: |
          mkdir -p "$EVIDENCE_DIR"
          required_commands=(
            a3s-gateway awk cargo comm curl docker git grep python3 realpath
            sha256sum sort timeout
          )
          : >"$EVIDENCE_DIR/required-commands.txt"
          for command in "${required_commands[@]}"; do
            if path=$(command -v "$command"); then
              printf '%s\t%s\n' "$command" "$path"
            else
              printf '%s\tMISSING\n' "$command"
            fi
          done >>"$EVIDENCE_DIR/required-commands.txt"

          {
            date -u '+%Y-%m-%dT%H:%M:%SZ'
            uname -a
            rustc --version
            cargo --version
          } >"$EVIDENCE_DIR/host-preflight.txt" 2>&1 || true
          docker version >"$EVIDENCE_DIR/docker-version.txt" 2>&1 || true
          docker info >"$EVIDENCE_DIR/docker-info.txt" 2>&1 || true
          docker ps --all --no-trunc \
            >"$EVIDENCE_DIR/docker-containers-preflight.txt" 2>&1 || true
          docker volume ls \
            >"$EVIDENCE_DIR/docker-volumes-preflight.txt" 2>&1 || true
          docker network ls \
            >"$EVIDENCE_DIR/docker-networks-preflight.txt" 2>&1 || true

          for command in "${required_commands[@]}"; do
            command -v "$command" >/dev/null
          done

          git -C crates/gateway rev-parse HEAD \
            >"$EVIDENCE_DIR/gateway.sha"
          grep --fixed-strings \
            "${{ steps.gateway_revision.outputs.revision }}" \
            "$EVIDENCE_DIR/gateway.sha"
          sha256sum "$(command -v a3s-gateway)" \
            >"$EVIDENCE_DIR/gateway-binary.sha256"
          a3s-gateway --version >"$EVIDENCE_DIR/gateway-version-command.txt"
          docker info >/dev/null

      - name: Run clean-host E0 release gate
        env:
          EVIDENCE_DIR: ${{ runner.temp }}/a3s-cloud-clean-host-e0-evidence
          RUNTIME_REVISION: ${{ steps.runtime_revision.outputs.revision }}
        run: |
          cloud_revision=$(git -C apps/cloud rev-parse HEAD)
          apps/cloud/tools/release-conformance/run_clean_host_gate.sh \
            --source-root "$GITHUB_WORKSPACE" \
            --cloud-sha "$cloud_revision" \
            --runtime-sha "$RUNTIME_REVISION" \
            --gateway "$(command -v a3s-gateway)" \
            --cargo "$(command -v cargo)" \
            --evidence-dir "$EVIDENCE_DIR" \
            --ttl-seconds 1800

      - name: Upload clean-host evidence
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: e0-clean-host-release-${{ github.run_id }}-${{ github.run_attempt }}
          path: ${{ runner.temp }}/a3s-cloud-clean-host-e0-evidence
          if-no-files-found: error
          retention-days: 30
