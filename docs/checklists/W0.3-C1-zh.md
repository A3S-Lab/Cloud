# W0.3-C1 Iteration internal claim path

## Goal

Advertise Workflow-owned `node.iteration` as `internal` through the existing
bounded-parallel Iteration foundation (ADR `0053` / Runtime v22). Do not invent
public availability or close full `W0.3` production foundation in this slice.

## Checklist

- [x] parity: `node.iteration` = `internal`, gate=`W0.3`
- [x] parity: gate `W0.3` remains `in_progress` (not `implemented` / `verified`)
- [x] claim path reuses composite-regions + wave coordinator evidence (no new runtime)
- [x] ADR `0242` + plan row + architecture note + README index
- [x] focused parity + workflow_node_catalog assertions green
- [x] refuse public `parity_claim`, Loop/Answer invent, BYOK/`S0` invent

## Non-goals

- Do not mark Loop or Answer `internal` here
- Do not move `W0.3` to `implemented` / `verified` / public
- Do not invent a second Flow/worker/scheduler authority

