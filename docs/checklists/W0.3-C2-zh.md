# W0.3-C2 ??Loop internal claim

## Scope

- Claim `node.loop` as `internal` under gate `W0.3`.
- Keep `W0.3` `in_progress`.
- Do not claim Answer or public availability.

## Evidence anchors

- ADR `0060` exact composite profile / sequential Loop
- `contracts/w0.3/composite-regions.acl`
- Coordinator `loop_tests.rs` + composite region domain tests
- New ADR `0243`

## Verification

- `cargo test -p a3s-cloud-contracts --test app_platform_parity_manifest`
- `cargo test -p a3s-cloud-control-plane --lib workflow_node_catalog`
- Profiles digest matches canonicalized parity manifest

