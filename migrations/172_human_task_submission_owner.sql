comment on table form_submissions is
    'Workflow-owned immutable HumanTaskSubmission evidence bound to one exact HumanTask generation; the historical table name preserves replay compatibility while Forms owns definitions, releases, and semantic evaluation';
